# Prism Constraint History

### 2026-07-12 — Buff icon positioning overhaul + Flurry merger (prism-full)
- **Maximized:** Anchor resolution across all 6 class CD/buff icon groups + 2 Flurry icons. Found Hunter is the only class using dynamic `GetDebuffAnchorBar()`; Flurry icons (Warrior/Shaman) use hardcoded 12px offset and never participate in the `GetDebuffStackOffset()` system.
- **Sacrificed:** X-axis centering semantics — "centered with actual bars" is a design preference, not a correctness bug. Recorded as user request.
- **Adversarial gain:** Flurry icon `SetFrameStrata("DIALOG")` renders on top of debuff bars when overlapping, making the overlap visually destructive. This was invisible in the analytical passes.
- **Resolution (Flurry):** Dedicated Flurry icon system removed (~100 lines). Flurry now tracked by unified CD/buff icon group for Warrior and Shaman.
- **Resolution (buff icon Y-position):** Root cause was shared-state corruption in `_debuffStackIconOffset` — `RestackDebuffBars` early-return paths (nil-reference-bar, alpha≤0 check) set the offset to nil, causing `GetDebuffStackOffset()` to return the default -8 fallback even when debuff bars were visible. Fixed by replacing `GetDebuffStackOffset()` with `ComputeBuffIconOffset(barList, referenceBar)` in all 6 classes — same synthetic arithmetic, zero shared mutable state, computed fresh each frame from the exact same bar list that `RestackDebuffBars` just positioned.
- **Shield Block:** Removed from debuff bar stack; now tracked as a CD/buff icon in the Warrior group (spellId=2565, label="SB").
---

### 2026-07-06 — GetHarmfulAuraData in SuperSwingTimer_ClassMods.lua
- **Maximized:** Shape discrimination analysis — identified that the boolean gate `type(pos3) == "string"` cannot distinguish TBC Anniversary 2.5.5 (AuraUtil.UnpackAuraData, icon-as-number) from Classic 1.13.x (count-as-number), causing spellId to be extracted from the wrong position.
- **Sacrificed:** Testing adequacy analysis — did not examine why no integration test caught the TBC Anniv nil-silently on first login, or which of the 24 callers actually depend on correct spellId.
- **Recommendations:** /prism-3way for caller-site impact tracing; /prism-full for test-adequacy analysis; /prism-scan with "positional API return across all WoW clients" focus
---

### 2026-07-14 — Buff icon debuff-bar Y-offset (prism-discover + prism-reflect)
- **Context:** User reports buff icons STILL don't adjust when debuff bars come up; prior sessions attempted fixes (shared-state removal done 2026-07-12) but problem persists. Dual-engine requirement: Classic Era 1.15.8 + TBC Anniversary 2.5.5/2.5.6 (incoming 2.5.6). SoD on same 1.15 patch.
- **Maximized:** Found the REMAINING defect the 07-12 fix missed — **divergent bar-list literals**. `RestackDebuffBars` (moves bars) and `ComputeBuffIconOffset` (positions icons) are called with DIFFERENT bar lists per class. Only **Warrior** diverges: restack gets 5 bars `{deepWounds, sunder, rend, thunderClap, demoShout}` (line 2863) but compute gets only 2 `{deepWounds, sunder}` (line 3106). When only rend/thunderClap/demoShout are visible (Fury/Arms w/o Deep Wounds+Sunder), compute sees 2 hidden bars → returns default 8 → icons stay 8px above mhBar.TOP and OVERLAP the stacked debuff bars. Paladin/Shaman/Druid/Hunter/Rogue pass identical lists (verified) → only Warrior is broken.
- **Sacrificed:** Lifecycle/reload behavior (mhBar recreation on UI reload / spec swap; nil-guard consistency across the Shaman fail-open pattern at line 8440). Visual/alpha overlap (ComputeBuffIconOffset checks IsShown not GetAlpha, so a fading-in bar counts as visible and icons jump early). Performance/frame-cost not measured (6 OnUpdate hooks each recompute offsets/frame).
- **Confirmed-not-the-bug (retraction of prior suspicion):** The orphaned `_debuffStackIconOffset` / `GetDebuffStackOffset` (lines 36,43,126,128) is DEAD — only the WoWUnit test harness reads it (Tests.lua:128,136,140). No production reader exists. It is refactor residue from the 07-12 fix, NOT the live cause. Tests assert the dead path → false green (StackOffset_* passes while real bug lives in ComputeBuffIconOffset).
- **Dual-engine toc (post-prism only):** `## Interface: 20505` is TBC-only → addon will NOT load on Classic Era 1.15.8 (20505 > 11508). FIX = comma-delimited same line `## Interface: 11508, 20505` (Warcraft Wiki confirms multiple Interface values on one line supported). KEEP 20505 (do not remove). This is a CRITICAL dual-engine blocker — entire addon (icons included) missing on Classic Era.
- **Conservation law (subsystem):** Stacking_Accuracy × Maintainability = 1. Two copies of the same stacking loop (restack vs compute) = drift guaranteed (Warrior proves it). Centralizing the list fixes accuracy but makes one bug global.
- **Meta-conservation law (analysis):** What_An_Analysis_Reveals × What_It_Conceals = Constant. Structural lens saw the loop, hid lifecycle + visual/alpha.
- **Resolution queue (NOT yet applied — prisms first, then fix):** (1) Warrior line 3106 → pass full 5-bar list matching 2863. (2) toc → `## Interface: 11508, 20505`. (3) Delete orphaned `_debuffStackIconOffset`/`GetDebuffStackOffset` + their Tests.lua assertions (dead code). (4) Fix doc comment line 50-51 ("negative"→positive, Y-up). (5) Update CHANGELOG + README (docs only). No code fix applied yet this session.
---
